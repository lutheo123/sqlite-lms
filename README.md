# sqlite-lms
SQLite LMS Project is a database for a learning management system. A project to improve the training and knowledge at Conpec.

Uses the 'SQLite3 Editor' VSCode Extention for data and query visualization, that's why every .sql file has a comment at the start.